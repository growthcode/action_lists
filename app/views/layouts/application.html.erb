<!DOCTYPE html>
<html>
  <head>
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= content_for?(:title) ? yield(:title) : "Action Lists" %></title>
    <meta name="description" content="<%= content_for?(:description) ? yield(:description) : "Action Lists" %>">
    <%= stylesheet_link_tag 'application', media: 'all' %>
    <%= javascript_include_tag 'application' %>
    <%= csrf_meta_tags %>
  </head>
  <body>
    <div id="flex-wrapper">
    <%= render 'layouts/navigation' %>
    <div id="main-content">
      <div class="main-wrapper">
        <%= render 'layouts/messages' %>
        <div class="content">
          <%= yield %>
        </div>
      </div>
    </div>
    </div>
  </body>
</html>
